import { testDB } from '$lib/data/testdb';

// export const load = async function () {
// 	const data = await testDB.find({}, { projection: { _id: 0 } }).toArray();

// 	console.log(data);

// 	return { testDB: data };
// };
